spring:
  flyway:
    baseline-on-migrate: true
  datasource:
    driver-class-name: org.postgresql.Driver
jwt:
  secret: ${JWT_SECRET:apiguard-backend-temp-secret-key-for-local-development-02037409}
  access-expiration: 3600000
  refresh-expiration: 1209600000
